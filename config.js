const APP_ROOT = 'https://aohcommunity.com';
const CLIENT_ID = 'test-demo-spa';

// Calculate frequently-used URLs from the above:
const APP_HOME = `${APP_ROOT}/index.html`;
const APP_CALLBACK = `${APP_ROOT}/redirect.html`;